const { MessageFlags } = require("discord.js");

const command = {
    name: 'vc'
    , description: 'Main voice chat command'
    , usage: `vc`
    , execute(message, args) {
        
    }
};

module.exports = command;